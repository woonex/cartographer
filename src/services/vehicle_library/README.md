# Vehicle Library

Represents the specific things going on right now with individual vehicles. This represents a standin for an OTA data piece transmitted back to the central server. Alternatively, if the stack was run locally, it could be a connection over the OBDII port/CAN of a system query.
